# JobLens — Job Market Insights & Salary Prediction System
> Capstone Project | Data Engineering + Data Science / ML

---

## Project Structure

```
job-market-insights/
├── notebook/
│   └── job_market_pipeline.ipynb    # Full ML pipeline
├── data/
│   ├── ds_salaries.csv              # Raw dataset (download below)
│   └── cleaned_data.csv             # Generated by notebook
├── outputs/
│   ├── predictions.csv              # Regression model outputs
│   ├── clusters.csv                 # K-Means cluster assignments
│   └── dashboard_data.json          # Frontend-ready JSON
├── frontend/
│   └── index.html                   # Vercel-deployable UI
└── README.md
```

---

## Dataset

**Source:** [Data Science Salaries 2023 — Kaggle](https://www.kaggle.com/datasets/arnabchaki/data-science-salaries-2023)

**File:** `ds_salaries.csv`  
**Size:** ~3,700 rows | 11 columns  
**Key columns:** `job_title`, `experience_level`, `employment_type`, `salary_in_usd`, `remote_ratio`, `company_size`, `company_location`

Download and place in `/data/` before running the notebook.

---

## Setup & Execution

### 1. Install dependencies
```bash
pip install pandas numpy scikit-learn jupyter
```

### 2. Run the notebook
```bash
cd notebook
jupyter notebook job_market_pipeline.ipynb
```
Run all cells top to bottom. Outputs are saved automatically.

### 3. Deploy frontend
- Recommended production root: `JobLens_Capstone_Project/project_full/frontend`
- Working CLI deploy command: `npx vercel deploy JobLens_Capstone_Project/project_full/frontend --prod --yes --public --force`
- Live production URL: https://frontend-iofk7l8ws-sujoys-projects-01c97fec.vercel.app
- Live alias URL: https://frontend-self-three-d4teea7mxf.vercel.app
- If you use the Vercel dashboard, set the project root directory to `JobLens_Capstone_Project/project_full/frontend` instead of the repository root
- The app loads `dashboard_data.json` with a relative fetch, so the file must stay beside `index.html`
- The live dashboard now shows 2,536 cleaned roles from the exported JSON, so the hero copy and stats stay consistent
- The predictor is strict about unsupported titles; it shows a no-match state instead of inventing a salary
- For a local demo, open `JobLens_Capstone_Project/project_full/frontend/index.html` directly in a browser

The notebook writes `dashboard_data.json` into both `/outputs/` and `/frontend/`, but only the frontend copy is needed for deployment.

---

## ML Models

| Model | Type | Metric |
|---|---|---|
| Gradient Boosting Regressor | Regression (Salary) | MAE ~$35.7k, R²~0.47 |
| K-Means (k=5) | Clustering (Job Groups) | Silhouette ≈ 0.43 |

---

## Tech Stack

| Layer | Technology |
|---|---|
| Data Processing | Python, Pandas, NumPy |
| ML | scikit-learn (GBR, KMeans, PCA, LabelEncoder) |
| Notebook | Jupyter |
| Frontend | HTML5, CSS3, Vanilla JS, Chart.js |
| Deployment | Vercel (static) |

---

## Features

- **Salary Predictor** — Input role + experience + company size → predicted salary range
- **Cluster Viewer** — PCA scatter plot of 500 role samples across 5 job clusters
- **Market Analytics** — Median salary by experience, top-10 job titles bar chart
- **Similar Jobs** — Roles in the same predicted cluster shown post-prediction
- **Zero backend** — All inference uses pre-exported JSON; runs on Vercel free tier
- **Input validation** — Unsupported or vague titles are rejected clearly instead of being forced into a random fallback

---

## Authors
Capstone Submission | B.Tech CSE
